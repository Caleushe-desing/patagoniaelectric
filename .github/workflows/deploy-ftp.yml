name: Deploy to hosting (FTP nativo)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Subir sitio por FTP
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Subir archivos al hosting por FTP nativo
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_PORT: ${{ secrets.FTP_PORT }}
          FTP_SERVER_DIR: ${{ secrets.FTP_SERVER_DIR }}
        run: |
          python - <<'PY'
          import ftplib
          import os
          import time
          from pathlib import Path
          from io import BytesIO

          local_root = Path(".").resolve()
          remote_root = (os.environ.get("FTP_SERVER_DIR") or "/public_html").strip()
          # La app Node/Passenger de Patagonia vive en /patagonia (cPanel nodevenv).
          if remote_root.rstrip("/") in {"/public_html", "public_html", "/"}:
              remote_root = "/patagonia"
          if not remote_root.startswith("/"):
              remote_root = f"/{remote_root}"

          host = os.environ["FTP_SERVER"].strip().replace("ftp://", "").replace("ftps://", "").rstrip("/")
          port = int(os.environ.get("FTP_PORT") or 21)
          user = os.environ["FTP_USERNAME"]
          password = os.environ["FTP_PASSWORD"]

          EXCLUDE_DIRS = {".git", ".github", "node_modules", "__pycache__"}
          EXCLUDE_FILES = {".env"}
          EXCLUDE_SUFFIXES = {".log"}

          MAX_ATTEMPTS = 5
          TIMEOUT = 120

          def should_skip(path: Path) -> bool:
              parts = set(path.parts)
              if parts & EXCLUDE_DIRS:
                  return True
              if path.name in EXCLUDE_FILES:
                  return True
              if path.suffix in EXCLUDE_SUFFIXES:
                  return True
              # Contenido CMS vivo en el hosting: no sobrescribir auth/mensajes
              if path.parts[0] == "data" and path.name in {"auth.json", "messages.json"}:
                  return True
              # content.json se maneja aparte (solo sembrar si no existe)
              if path.parts[0] == "data" and path.name == "content.json":
                  return True
              if path.parts[:2] == ("public", "uploads") and path.name != ".gitkeep":
                  return True
              return False

          def remote_exists(ftp, remote_path):
              try:
                  ftp.size(remote_path)
                  return True
              except Exception:
                  return False

          def connect():
              ftp = ftplib.FTP(timeout=TIMEOUT)
              print(f"Conectando a {host}:{port} ...")
              ftp.connect(host, port)
              ftp.login(user, password)
              ftp.set_pasv(True)
              return ftp

          def ensure_dir(ftp, remote_path):
              current = ""
              for part in remote_path.strip("/").split("/"):
                  current += f"/{part}"
                  try:
                      ftp.mkd(current)
                  except ftplib.error_perm as error:
                      if not str(error).startswith("550"):
                          raise

          def upload_file(ftp, local_file, remote_path):
              with local_file.open("rb") as file:
                  print(f"Subiendo {local_file} -> {remote_path}")
                  ftp.storbinary(f"STOR {remote_path}", file)

          def upload_dir(ftp, local_dir, remote_dir):
              ensure_dir(ftp, remote_dir)
              for item in sorted(local_dir.iterdir()):
                  rel = item.relative_to(local_root)
                  remote_path = f"{remote_dir.rstrip('/')}/{item.name}"

                  # Semilla CMS: solo subir content.json si aún no existe en el servidor
                  if tuple(rel.parts) == ("data", "content.json"):
                      ensure_dir(ftp, remote_dir)
                      if remote_exists(ftp, remote_path):
                          print(f"Preservando contenido del editor: no sobrescribir {remote_path}")
                      else:
                          print(f"Sembrando contenido inicial (primera vez): {remote_path}")
                          upload_file(ftp, item, remote_path)
                      continue

                  if should_skip(rel):
                      continue
                  if item.is_symlink():
                      print(f"Omitiendo symlink {item}")
                      continue
                  if item.is_dir():
                      upload_dir(ftp, item, remote_path)
                      continue
                  upload_file(ftp, item, remote_path)

          def deploy():
              ftp = connect()
              try:
                  print(f"Usando remote_root={remote_root}")
                  upload_dir(ftp, local_root, remote_root)

                  restart_dir = f"{remote_root.rstrip('/')}/tmp"
                  ensure_dir(ftp, restart_dir)
                  restart_path = f"{restart_dir}/restart.txt"
                  print(f"Tocando {restart_path} para reiniciar Passenger")
                  ftp.storbinary(f"STOR {restart_path}", BytesIO(f"restart {time.time()}\n".encode("utf-8")))

                  marker = f"{remote_root.rstrip('/')}/public/css/portfolio.css"
                  size = ftp.size(marker)
                  print(f"Verificación OK: {marker} ({size} bytes)")
              finally:
                  try:
                      ftp.quit()
                  except Exception:
                      pass

          last_error = None
          for attempt in range(1, MAX_ATTEMPTS + 1):
              try:
                  print(f"== Intento de despliegue {attempt}/{MAX_ATTEMPTS} ==")
                  deploy()
                  print("Despliegue FTP completado correctamente.")
                  last_error = None
                  break
              except Exception as error:
                  last_error = error
                  wait = attempt * 20
                  print(f"Fallo el intento {attempt}: {error}")
                  if attempt < MAX_ATTEMPTS:
                      print(f"Reintentando en {wait}s ...")
                      time.sleep(wait)

          if last_error is not None:
              raise SystemExit(f"Despliegue FTP fallido tras {MAX_ATTEMPTS} intentos: {last_error}")
          PY
