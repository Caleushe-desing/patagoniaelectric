<div class="admin-repeater__header">
  <div>
    <h3 class="admin-repeater__title"><%= title %></h3>
    <% if (typeof hint !== 'undefined') { %><p class="admin-repeater__hint"><%= hint %></p><% } %>
  </div>
  <span class="admin-repeater__count" data-count-for="<%= field %>">0</span>
</div>
